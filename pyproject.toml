[project]
name = "pulumi-kind"
version = "0.1.0"
description = "Local Kind cluster for pulumi-api and monitoring"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pulumi>=3.219.0",
    "pulumi-command>=1.1.3",
]
