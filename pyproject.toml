[project]
name = "pulumi-k8s-kind"
version = "0.1.0"
description = "Create a local k8s cluster using kind with Pulumi and uv"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-command>=1.0.0,<2.0.0",
    "pulumi-kubernetes>=4.0.0,<5.0.0",
]
