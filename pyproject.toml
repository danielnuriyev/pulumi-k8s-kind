[project]
name = "pulumi-k8s-kind"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Daniel Nuriyev" }
]
requires-python = ">=3.14"
dependencies = [
    "pulumi>=3.219.0",
    "pulumi-command>=1.1.3",
    "pulumi-aws>=6.0.0",
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
